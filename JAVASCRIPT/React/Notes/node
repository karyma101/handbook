NodeJS
  -JavaScript runtime that uses the v8 javascript engine
  -Allows us to run JS on the server
  -Used to build very fast and scalable real-time application
  -Uses an event driven, non blocking I/O model

Blocking Model
  -Upload the File
  -Display the File
  -Do Something Else

Non Blocking(Node)
  -Asynchronous
  -Upload the File
  -When it's done do something else
  -Do something else